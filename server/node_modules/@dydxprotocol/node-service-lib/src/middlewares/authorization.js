export default function (authorizationHeader) {
  return (req, res, next) => {
    if (req.headers.authorization !== authorizationHeader) {
      res.sendStatus(401);
      return;
    }
    next();
  };
}
